@{
    ViewBag.Title = "ListarCategoriasPersonajes";
}

<div class="jumbotron text-center" style="background-color:indigo">
    <h1 style="color:white;font-size:400%">Listado de categorias de personajes</h1>
</div>

<!--@Html.ActionLink("Agregar categoria de personajes", "AgregarCategoriaPersonaje", "BackOffice", new { @class = "btn btn-info" }) Forma con ActionLink-->
<a id="AgregarCat" class="btn btn-info" href="@Url.Action("AgregarCategoriaPersonaje", "BackOffice")">Agregar categoria de personajes</a> <!--Misma forma pero con <a>, asi le puedo poner un id y despues un style-->
<style>
    table, th, td {
        border: 1px solid black;
    }

    p {
        margin-top: -1%;
    }

    hr {
        margin-top: 170px;
        margin-bottom: 20px;
        border: 0;
        border-top: 1px solid #eee;
    }

    th {
        background-color: violet;
        font-size: 18px;
    }

    td {
        background-color:gainsboro;
        font-size: 16px;
    }

    h1 {
        font-family: Arial, sans-serif;
        font-weight: 900;
        -webkit-text-stroke: 2px #003326;
        text-shadow: 3px 5px rosybrown;
    }

    body {
        padding-top: 2px;
        background-color: lightsteelblue;
    }


    #tabla {
        width: 300px;
        margin: 0 auto;
    }

    #AgregarCat {
        margin: auto;
        display: block;
        width: 300px;
    }
</style>

<table id="tabla" class="table table-bordered">
    <tr>
        <th>Categoria</th>
        <th></th>
    </tr>
    @foreach (int idCatPers in BD.dicCategorias.Keys)
    {
        string CatPers = BD.dicCategorias[idCatPers];
        <tr>
            <td>@CatPers</td>
            <td>@Html.ActionLink("Borrar categoria", "BorrarCategoriaPersonajes", "BackOffice", new { sCatPersonaje = CatPers }, new { @class = "btn btn-info" })</td>
        </tr>
    }
</table>