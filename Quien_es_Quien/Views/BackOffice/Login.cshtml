@{
    ViewBag.Title = "Login";
    if (ViewBag.Mensaje != null)
    {
        Response.Cookies["Mensaje"].Value = ViewBag.Mensaje;
        Response.Cookies["Mensaje"].Expires = DateTime.Now.AddHours(1);
    }
}

<script>
    window.onload = function () {
        var msg = getCookieValue("Mensaje");
        if (msg != "") {
            alert(msg);
            document.cookie = "Mensaje=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";
        }
    }

    function getCookieValue(cname) {
        var name = cname + "=";
        var decodedCookie = decodeURIComponent(document.cookie);
        var ca = decodedCookie.split(';');
        for (var i = 0; i < ca.length; i++) {
            var c = ca[i];
            while (c.charAt(0) == ' ') {
                c = c.substring(1);
            }
            if (c.indexOf(name) == 0) {
                return c.substring(name.length, c.length);
            }
        }
        return "";
    }
</script>

<style>
    body {
        padding-top: 5px;
        background-color: lightsteelblue;
    }

    .spacer {
        margin-top: 2%;
        margin-bottom: 2%;
    }

    .block { /* este es el largo de la pagina*/
        height: 320px;
        padding-top: 15px;
        background-color: lightsteelblue;
    }

    .block2 {
        min-height: 160px;
        padding-top: 35px;
    }

    .center {
        position: absolute;
        /*  top: 0;
    bottom: 0; */
        left: 0;
        right: 0;
        margin: auto;
    }

    h1 {
        font-family: Arial, sans-serif;
        font-weight: 900;
        -webkit-text-stroke: 2px #003326;
        text-shadow: 3px 5px rosybrown;
    }
</style>

<body>

    <div class="jumbotron text-center" style="background-color:indigo">
        <h1 style="color:white;font-size:650%">Login</h1>
    </div>

    <div class="container col-lg-12 block">
        <div class="row col-xs-6 block2 bg-primary center">
            <form name="input" action="@Url.Action("Login", "BackOffice")" method="post" class="form-horizontal" role="form" align="center">
                <div class="form-group">
                    <label class="control-label col-sm-3" for="nombre">Nombre de usuario<em>*</em></label>
                    <div class="col-sm-8 col-xs-12">
                        <input required type="text" name="nombre" id="username" placeholder="Cebo" class="form-control" />
                    </div>
                </div>
                <div class="form-group">
                    <label class="control-label col-sm-3" for="contrasenia">Contraseña<em>*</em></label>
                    <div class="col-sm-8 col-xs-12">
                        <input required type="password" name="contrasenia" id="password" placeholder="*********" class="form-control" />
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-sm-offset-5 col-sm-8">
                        <input type="submit" name="Submit" value="Enviar" class="btn btn-info" />
                    </div>
                </div>
            </form>
        </div>
    </div>

</body>